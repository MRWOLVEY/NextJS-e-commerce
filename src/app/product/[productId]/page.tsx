import ProductClient from "./ProductClient";

export default function ProductPage() {
  // This is a placeholder page component
  // The actual implementation is now in ProductClient.tsx
  // The layout.tsx will render the ProductClient with the proper props
  return null;
}
